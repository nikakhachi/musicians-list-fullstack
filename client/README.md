# Local JSON DataBase Manipulations

### Simple Program written in React, which uses local database ( db.json ).

#### Requests are done with fetch and id-s are generated using react-uuid. Primitive, raw inline CSS is used
